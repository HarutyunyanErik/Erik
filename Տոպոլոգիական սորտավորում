graph = {
    "A": ["C"],
    "B": ["C", "D"],
    "C": ["E"],
    "D": ["F"],
    "E": ["F"],
    "F": []
}

def topo_sort(graph):
    visited = set()
    stack = []

    def dfs(v):
        visited.add(v)
        for n in graph[v]:
            if n not in visited:
                dfs(n)
        stack.append(v)

    for v in graph:
        if v not in visited:
            dfs(v)

    return stack[::-1]

print(topo_sort(graph))
